package org.personal.mason.feop.oauth.contact.domain.repository;

import org.personal.mason.feop.oauth.contact.domain.model.ContactInstantMessage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ContactInstantMessageRepository extends JpaRepository<ContactInstantMessage, Long> {

}
